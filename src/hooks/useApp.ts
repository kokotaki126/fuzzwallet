const useApp = () => {
  const initAccount = () => {
    // By Tg
    // By Google
  };

  const initBalance = () => {};

  const initMainPageActs = () => {};

  // Load Recent Transactions
  const loadWalletTransaction = () => {};

  const redirectTo = () => {};

  return {
    initAccount,
    initBalance,

    initMainPageActs,
    loadWalletTransaction,
    redirectTo,
  };
};

export default useApp;

